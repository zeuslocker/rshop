<%= form_for(product) do |f| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.text_field :price %>
  </div>

  <div class="field">
    <%= f.label :category_type %>
    <%= f.select :category_type, Category.all.map{|x| x.title}, {include_blank: false} %>
  </div>
  <div class="field">
    <%= f.label :subcategory %>
    <%= f.text_field :subcategory %>
  </div>

    <div class="field">
        <%= f.label :images_links %>
        <%= f.text_area :images_links %>
    </div>
    <br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
